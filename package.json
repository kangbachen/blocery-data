{
  "name": "blocery-data",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ kangbachen/blocery-data",
    "create-local": "graph create --node http://localhost:8020/ kangbachen/blocery-data",
    "remove-local": "graph remove --node http://localhost:8020/ kangbachen/blocery-data",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kangbachen/blocery-data"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
